# shared

Shared schemas, protocol contracts, and common utilities used across components.

Current packages:

- `shared/protocol` typed protocol envelopes, messages, and signal schema contracts.
- `shared/security/secretstore` file-permission-safe credential storage fallback.
